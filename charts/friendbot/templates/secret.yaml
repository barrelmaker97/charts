{{ if .Values.signature }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-signature
  labels:
    app: friendbot
type: Opaque
stringData:
  signature: "{{ .Values.signature }}"
{{- end }}
